# This function takes arguments (inputs).
# The name inside parentheses is a parameter.
# It allows us to customize the output.

def greet_user(name):
    # We use the parameter inside the function
    print("Hello,", name + "!")

# Now we call the function and pass different values
greet_user("Arsen")
greet_user("Alua")
